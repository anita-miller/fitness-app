<!DOCTYPE html>
<html>
<head>
    <title>Digicache</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <link href="css/Ranking.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>

<div class ="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/"><h4>Digicache</h4></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav text-info">
                <li><a href="/map"><h4>Map</h4></a></li>
                <li><a class="text-info" href="/profile"><h4>Profile</h4></a></li>
                <li class="active"><a href="/Ranking"><h4>Ranks</h4><span class="sr-only">(current)</span></a></li>
            </ul>
        </div>
    </div>
</div>





<div class="container">
    <div class="table-responsive col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-1 main">

        <h1 class="page-header text-primary table-text-header">Rankings</h1>
        <div class="table-responsive background">
            <table class="table table-striped table-dark">
                <thead class="thead-light">
                <tr>
                    <th class="table-text"><h4>Rank</h4></th>
                    <th class="table-text"><h4>Name</h4></th>
                    <th class="table-text"><h4>Score</h4></th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td><h3><%= userRank %></h3></td>
                    <td><h3><%= user.local.fname %> <%= user.local.lname %></h3></td>
                    <td><h3><%= user.data.score %></h3></td>
                </tr>
                <% for(var i = 0; i < 100 && i < sortedUsers.length; i++) { %>
                <% if(sortedUsers[i].data.id != user.data.id){ %>
                <tr>
                    <td><h4><%= i %></h4></td>
                    <td><h4><%= sortedUsers[i].local.fname %> <%= sortedUsers[i].local.lname %></h4></td>
                    <td><h4><%= sortedUsers[i].data.score %></h4></td>
                </tr>
                <%}%>
                <%}%>
                </tbody>
            </table>
        </div>
    </div>
</div>



</body>
</html>
